<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="menu">
    <!--모든 음식메뉴 정보 가져오기-->
    <select id="selectAllMenu" resultType="mybatis.vo.MenuVO">
        SELECT SAKey, FoodName, Price, Recommend, Best, Premium, Season
        FROM Menu
    </select>

    <!--음식메뉴 정보 INSERT-->
    <insert id="insertMenu" parameterType="mybatis.vo.MenuVO">
        INSERT INTO Menu (saKey, foodName, price, recommend, best, premium, season)
        VALUES (#{SAKey}, #{FoodName}, #{Price}, #{Recommend}, #{Best}, #{Premium}, #{Season})
    </insert>

    <!--음식메뉴 정보 UPDATE-->
    <update id="updateMenu" parameterType="mybatis.vo.MenuVO">
        UPDATE Menu
        SET price = #{Price},
        recommend = #{Recommend},
        best = #{Best},
        premium = #{Premium},
        season = #{Season}
        WHERE saKey = #{SAKey} AND foodName = #{FoodName}
    </update>
</mapper>